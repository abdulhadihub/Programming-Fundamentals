#include <iostream>
#include <stdio.h>
#include <conio.h>

using namespace std;

int main()
{
    // char alphabet = 'x';
    // int i = 34;
    // float x = 3.15194;
    // double d = 3453243.933220394;
    // printf("char: %c \nfloat: %f", alphabet, x);
    // printf("\ndouble: %1f \ninterger: %d", d, i);
    // printf("\nstring: %s", "sample string");
    // getch();

    double a = 1;
    double b = 1;
    double c = 1;
    double d = 1;
    double q = 1;
    double r = 1;
    double x = 1;
    double y = 1;
    double v = 1;
    double m = 1;
    double g = 1;

    double z = (((8.8 * (a + b) * (2 / c)) - (0.5 + ((2 * a) / (q + r)))) / ((a + b) * (1 / m)));
    double X = (((-b) + (b * b) + (2 * 4 * a * c)) / (2 * a));
    double R = (((2 * v) + (6.22 * (c + d))) / (g + v));
    double A = ((((7.7 * b) * ((x * y) + a)) / (c - 0.8 + (2 * b))) / ((x + a) * (1 / y)));

    return 0;
}